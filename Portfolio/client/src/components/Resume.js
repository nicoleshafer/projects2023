import React from 'react';
import resume from "../images/resume.png"
const Resume = () => {
    return (
        <div>
            <img src={resume} alt="Nicole Shafer's Resume"></img>
        </div>
    );
}

export default Resume;
